variable res : entero

funcion aux (base : entero, expoente : entero) : entero
    variable res : entero
    res = base ^expoente;
    devuelve res.

funcion potencia() : entero
    variable base, expoente,resultado : entero
    base = entero(lee("base: "));
    expoente = entero(lee("expoente: "));
    resultado = aux(base,expoente);
    devuelve resultado.

res = aux();
escribe(res);

    
