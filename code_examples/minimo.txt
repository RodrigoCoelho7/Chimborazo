variable N,count = 1 : entero
variable val,min : real

N = entero(lee("N: "));
val = real(lee("val: "));
min = val;

para count<N siguiente count = count +1 hacer
    val = real(lee("val: "));
    si val < min entonces
        min = val;..

escribe(min);

