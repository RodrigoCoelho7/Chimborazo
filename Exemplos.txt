variable count, N : entero
count = 0
N = 10
para count < N siguiente count +=1.



variable x,y : real
x = 1.5
y = 2.7
x,y = y,x



funcion length(list:lista) : entero
    variable count = 1 : entero
    enquanto list[count] diferente nada hacer
        count +=1
    devuelve count



variable list : lista
list = [1,2,3,[]]

variable N : entero
N = length(lista)



funcion is-even (N : entero) : booleano
    si N resto 2 igual 0 entonces
        escribe("is even")
        devuelve Verdadero
    si no
        escribe("is odd")
        devuelve Falso
